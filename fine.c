#include<stdio.h>

int main(){
    int t,n,m;
    scanf("%d",&t);
    char s[2];

    for(int i=1;i<=t;i++)
    {
        scanf("%d",&n);

        for(int j=0;j<n;j++)
        {
            scanf("%d %s",&m,s);
        }

        for(int k=0;k<strlen(s);k++)
        {
           ;
        }
    }
    return 0;
}